import type { Invoice } from '../domain/invoice'

export const createInvoice = (invoice: Invoice) => {
  console.log('Invoice created:', invoice)
  // Placeholder to send to backend later
  // Aquí iría la lógica de validación o envío
}